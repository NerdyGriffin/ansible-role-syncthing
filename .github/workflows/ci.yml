---
name: CI

'on':
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    # Check out under the namespaced role name so ansible-lint's role-name rule
    # resolves the role the way a Galaxy install presents it.
    defaults:
      run:
        working-directory: 'nerdygriffin.syncthing'
    steps:
      - name: Check out the codebase.
        uses: actions/checkout@v5
        with:
          path: 'nerdygriffin.syncthing'

      - name: Set up Python 3.
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Install test dependencies.
        run: pip3 install yamllint ansible-lint

      - name: Lint YAML.
        run: yamllint .

      - name: Lint Ansible.
        run: ansible-lint .
